# --- Kişisel veri & sırlar (ASLA commit edilmez) ---
# Private veri reposu bu klasörün içine klonlanıyor (profil, CV, state, loglar).
# Bir `git add .` bunu PUBLIC repoya taşımasın diye tamamen dışlanır.
outreachos-data/
tracker.db
state.json
outreach_log.csv
outreach_tracker.xlsx
credentials.json
token.json
.env
cv/
gunluk_ozet/

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- OS ---
.DS_Store
Thumbs.db

# --- Claude Code local settings & eval scratch (not for public repo) ---
.claude/settings.local.json
.claude/skills/job-search-cloud-agent-workspace/
